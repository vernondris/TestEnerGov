import { EmployeeDataToBeSave } from './employee-data-to-be-save.model';

describe('EmployeeDataToBeSave', () => {
  it('should create an instance', () => {
    expect(new EmployeeDataToBeSave()).toBeTruthy();
  });
});
